var expect = chai.expect;

describe('Do Mocha Runs?', function() {

  // add a test hook
  beforeEach(function() {
    // ...some logic before each test is run
  })
  
  // test a functionality
  it('Expect should pass without errors', function() {
    // add an assertion
    expect(1).to.equal(1);
  })
  
  // ...some more tests
  
})